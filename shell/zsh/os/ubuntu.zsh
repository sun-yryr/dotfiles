# Ubuntu固有のパス設定
export PATH="$HOME/.local/bin:$PATH"
export ZPLUG_HOME="$HOME/.zplug"

# Ubuntu固有のエイリアス
alias open='xdg-open'
alias pbcopy='xclip -selection clipboard'
alias pbpaste='xclip -selection clipboard -o'

# swiftly
. "/home/sun-yryr/.local/share/swiftly/env.sh"
